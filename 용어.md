# 용어

## 완전제곱식

$(a+b)^2$ 또는 $(a-b)^2$ 꼴로 인수분해되는 식. 식 전체가 제곱으로 표현되는 식이므로 완전제곱식이라고 불린다.

## 변분법
변분법이란 미적분학의 한 분야로, 일반 미적분학과는 달리 범함수를 다룬다. 이런 미적분학은 알려지지 않은 함수와 이 함수의 도함수를 다루는데, 주로, 어떠한 값을 최대화 하거나, 최소화하는 함수 모양이 어떻게 되는가를 다룬다.

## functional
범함수(functional)란 함수의 함수로, 함수의 집합에서 정의된 함수이다. 예를 들어, $F[f]$는 $f$라는 함수를 입력으로 받아서 어떤 값을 출력하는 범함수이다. 범함수는 주로 변분법에서 사용되며, 최적화 문제에서 특정 함수의 형태를 찾는 데 사용된다.

## 라그랑주 승수법
라그랑주 승수법은 최적화 문제에서 제약 조건이 있는 경우에 사용되는 방법이다. 이 방법은 최적화하려는 함수와 제약 조건을 결합하여 새로운 함수를 만들고, 이 함수의 극값을 찾는 방식으로 문제를 해결한다. 라그랑주 승수법은 최적화 문제에서 매우 유용하며, 다양한 분야에서 활용된다.


## 쌍대문제 (dual problem)

쌍대문제(dual problem)는 원래의 최적화 문제를 다른 관점에서 다시 표현한 문제이다. 보통 원문제(primal problem)를

$$
\begin{aligned}
\min_x \quad & f(x) \\
\text{s.t.} \quad & g_i(x)\le 0, \quad i=1,\dots,m, \\
& h_j(x)=0, \quad j=1,\dots,k
\end{aligned}
$$

와 같이 놓고, 라그랑지안

$$
L(x,\lambda,\nu)=f(x)+\sum_{i=1}^m \lambda_i g_i(x)+\sum_{j=1}^k \nu_j h_j(x)
$$

을 정의한다. 여기서 $\lambda_i\ge 0$이고, $\nu_j$는 등식 제약의 승수이므로 부호 제한이 없다. 이때

$$
q(\lambda,\nu)=\inf_x L(x,\lambda,\nu)
$$

를 쌍대함수라고 하고, 이를 최대화하는 문제

$$
\max_{\lambda,\nu}\ q(\lambda,\nu) \quad \text{s.t.} \quad \lambda_i\ge 0
$$

를 라그랑주 쌍대문제라고 한다.

쌍대문제는 원문제의 최적값을 하한으로 제공한다. 즉, 어떤 $\lambda\ge 0$에 대해서도

$$
q(\lambda,\nu)\le p^*
$$

가 성립하며, 가능한 하한 중 가장 큰 값을 찾는 것이 쌍대문제의 목적이다. 따라서 쌍대문제의 최적값을 $d^*$라 하면 항상

$$
d^*\le p^*
$$

이 성립한다. 이를 약한 쌍대성(weak duality)이라고 한다. 더 나아가 볼록성 및 정칙성 조건이 만족되면

$$
d^*=p^*
$$

가 되어 강한 쌍대성(strong duality)이 성립한다.

라그랑주 승수법과 쌍대문제는 같은 라그랑지안을 사용하지만 초점이 다르다. 라그랑주 승수법은 최적조건

$$
\nabla_x L=0, \quad \nabla_\lambda L=0
$$

을 풀어 최적점 후보를 찾는 데 초점을 두고, 쌍대문제는 승수 자체를 변수로 삼아 새로운 최적화 문제를 만든다. 즉, 라그랑주 승수법은 최적조건을 구하는 과정이고, 쌍대문제는 승수를 변수로 하는 최적화 문제를 구성하는 과정이다.

이처럼 쌍대문제는 제약 최적화의 일반적인 구조를 다른 관점에서 재해석하는 개념이며, 현재 다루는 쌍대문제는 정확히 말해 라그랑주 쌍대문제이다. 최적화 이론 전반에서 선형계획법, 볼록최적화, 게임이론, 최소비용 흐름 등 다양한 분야에서 쌍대성 개념이 널리 사용된다.
