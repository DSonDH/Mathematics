# 일반화된 리만 적분

함수 $F$가 미분가능하다면 아래 식이 성립함을 증명할 수 있길 바라지만,

$$\int_a^b F' = F(b) -F(A)$$

$F'$이 리만적분 가능하다는 가정이 필요했다. 실제로, 모든 도함수가 적분가능한건 아니었다.

1960년경 야로슬라프 커츠와일(Jaroslav Kurzweil)과 랄프 헨스톡(Ralph Henstock)이 각각 독자적으로 발견한 '일반화된 리만 적분'이 있다. 이는 르벡적분보다 더 많은 함수를 적분할 수 있고, 추가 가정없이 위 식을 간결하게 증명할 수 있다.

## 극한으로서의 리만적분

$[a,b]$에서의 분할 $P = \{x_0, x_1, x_2, ..., x_n\}$ 이 있다.

**태그된 분할(tagged partition)** 은 분할 $P$에서 각 부분구간 $[x_{k-1}, x_k]$ 마다 점 $c_k$를 하나씩 선택하는 조건을 추가한 것이다.

이 분할로 생성된 리만 합(Riemann sum)은 $R(f,P) = \sum_{k=1}^n f(c_k)(x_k - x_{k-1})$ 이다.

### 정의. $\delta$-세분 ($\delta$-fine)
$\delta >0$ 에 대하여 분할 $P$의 모든 부분구간 $[x_{k-1}, x_k]$이 $x_k- x_{k-1} <\delta$를 만족하면 $P$를 $\delta$-세분 ($\delta$-fine)라 한다.

### 정리. 리만 적분가능성에 대한 극한 판정법

유계함수 $f: [a,b]\to \mathbb R$가 리만적분 가능하고 $\int_a^b f = A$이기 위한 필요충분조건은 임의의 $\epsilon >0$에 대해 다음 조건을 만족하는 $\delta >0$이 존재하는 것이다:

임의의 $\delta$-세분인 태그된 분할 $(P, \{c_k\})$에 대해 $|R(f,P)-A|<\epsilon$

**증명**

($\Rightarrow$)

$f$가 리만 적분 가능하므로 상합과 하합의 차이를 임의로 작게 만들 수 있다. 따라서 주어진 $\epsilon>0$에 대하여 다음을 만족하는 분할 $P_\epsilon$이 존재한다.

$$U(f,P_\epsilon)-L(f,P_\epsilon)<\frac{\epsilon}{3}.$$

이 $P_\epsilon$은 임의로 주어지는 분할 $P$와 구분하기 위한 기준 분할이다. 아래첨자 $\epsilon$은 이 분할이 $\epsilon$에 의존하여 선택되었다는 뜻이다. 구체적으로

$$P_\epsilon=\{t_0,t_1,\dots,t_n\},\quad a=t_0<t_1<\cdots<t_n=b$$

라고 쓰겠다. 그러면 $P_\epsilon$은 $n+1$개의 분할점과 $n$개의 부분구간 $[t_0,t_1],\dots,[t_{n-1},t_n]$ 으로 이루어진다.

상합과 하합을 각각 $U(f,P)=\sum_{k=1}^n M_k\Delta x_k,\ L(f,P)=\sum_{k=1}^n m_k\Delta x_k$ 로 나타내며,  
$M_k=\sup_{x\in[x_{k-1},x_k]}f(x),\ m_k=\inf_{x\in[x_{k-1},x_k]}f(x)$ 로 둔다.

---

문제 1  

(a) $R(f,P)$와 $\int_a^b f$가 $L(f,P)$, $U(f,P)$ 사이에 있는 이유

태그 $c_k\in[x_{k-1},x_k]$에 대하여 $m_k\le f(c_k)\le M_k$ 이다. $\Delta x_k=x_k-x_{k-1}>0$을 곱하면 $m_k\Delta x_k \le f(c_k)\Delta x_k \le M_k\Delta x_k$ 이다. 이를 모두 더하면 $L(f,P)\le R(f,P)\le U(f,P)$ 를 얻는다.

또한 $f$가 리만 적분 가능하면 하적분과 상적분이 모두 $\int_a^b f$와 같으므로, 임의의 분할 $P$에 대하여

$$L(f,P) \le \underline{\int_a^b}f =\int_a^b f =\overline{\int_a^b}f \le U(f,P)$$

(b) $U(f,P')-L(f,P')<\epsilon/3$인 이유

$P'=P\cup P_\epsilon$이므로 $P'$는 $P_\epsilon$의 세분이다. 분할을 세분하면 상합은 감소하고 하합은 증가하므로 $U(f,P')\le U(f,P_\epsilon), \ L(f,P')\ge L(f,P_\epsilon)$ 이다. 따라서 $U(f,P')-L(f,P') \le U(f,P_\epsilon)-L(f,P_\epsilon) <\frac{\epsilon}{3}$ 

--- 

문제 2: $P'$는 $P$의 세분이다. $U(f,P')\le U(f,P)$ 임을 보여라.

$P$의 한 부분구간 $I=[u,v]$ 가 $P'$에 의하여 $u=y_0<y_1<\cdots<y_r=v$ 로 분할되었다고 하자. $I$ 에서의 상한을 $M=\sup_{x\in I}f(x)$ 라 하고, 작은 부분구간 $[y_{j-1},y_j]$에서의 상한을 $M_j$ 라 하자. 작은 부분구간은 $I$에 포함되므로 $M_j\le M$ 이다. 따라서

$$\sum_{j=1}^r M_j(y_j-y_{j-1}) \le \sum_{j=1}^r M(y_j-y_{j-1}) =M(v-u)$$

즉, 하나의 구간을 세분하면 그 구간에 대응하는 상합은 증가하지 않는다. 모든 부분구간에 이 결과를 적용하면 $U(f,P')\le U(f,P)$ 이다. 그러므로

$$\boxed{U(f,P)-U(f,P')\ge0}$$

이다. 마찬가지로 작은 구간에서의 하한은 원래 구간에서의 하한보다 크거나 같으므로

$$\boxed{L(f,P')-L(f,P)\ge0}$$

---

문제 3

(a) $U(f,P)$, $U(f,P')$ 중 한쪽에만 나타나고 다른쪽에는 나타나지 않는 $M_k(x_k-x_{k-1})$ 꼴의 항의 개수?

$M_k$는 $[x_{k-1}, x_k]$에서 $f$의 상한이다. 이런 항 중에서 상당수는 두 상합 모두에 나타나므로 서로 상쇄된다 (즉, 새 분할이 생기지 않아 상합이 달라지지 않는 구간이 많다는 뜻).

$P_\epsilon$의 내부 분할점 $n-1$ 개 중 이미 $P$에 속하지 않는 점의 개수를 $q$ 라 하자. 그러면 $q\le n-1$ 이다.  
이 $q$ 개의 점이 들어 있는 $P$의 부분구간 개수를 $s$ 라 하자. 하나의 해당 부분구간에 $r_j$개의 새로운 점이 들어 있다면

* $U(f,P)$에서는 원래 구간에 대응하는 항이 1개이고,
* $U(f,P')$에서는 그 구간이 $r_j+1$개로 나뉘므로 항이 $r_j+1$개이다.

따라서 이 구간에서 상쇄되지 않는 항은 총 $r_j+2$ 개이다. 모든 해당 구간에 대하여 더하면 $\sum_{j=1}^s(r_j+2)=q+2s$여기서 $s\le q$이므로 $q+2s\le3q\le3(n-1)<3n$ 따라서 상쇄되지 않는 항의 개수는 최대 $3(n-1)$개이며, 필요한 간단한 상계는 $3n$ 개이다.

(b) $U(f,P)-U(f,P')<\epsilon/3$ 증명

$M>0$ 이 $|f|$ 의 상계이므로 모든 부분구간에서 $|M_k|\le M$ 이다. 또한 $P$가 $\delta$-세분이므로 $P$의 각 부분구간 길이는 $\delta$보다 작다. $P'$의 부분구간도 $P$의 부분구간에 포함되므로 그 길이 역시 $\delta$보다 작다.

따라서 상합을 구성하는 각 항의 절댓값은 $|M_k\Delta x_k|<M\delta$.

문제 2에서 $U(f,P)-U(f,P')\ge0$ 임을 알고 있으며, 문제 3(a)에 의해 상쇄되지 않는 항은 $3n$개 미만이다. 따라서

$$ 0 \le U(f,P)-U(f,P') <3nM\delta=3nM\frac{\epsilon}{9nM}=\frac{\epsilon}{3}$$

이다. 즉, $U(f,P)<U(f,P')+\frac{\epsilon}{3}$ 이다.  
하합에 대해서도 완전히 같은 방법을 적용하면

$$0\le L(f,P')-L(f,P)<\frac{\epsilon}{3}$$

이고, 따라서 $L(f,P')-\frac{\epsilon}{3}<L(f,P)$ 이다. 결국

$$\boxed{L(f,P')-\frac{\epsilon}{3}<L(f,P)\le U(f,P)<U(f,P')+\frac{\epsilon}{3}}$$

---
문제 4

이제 역방향을 증명하면 된다. $\epsilon-\delta$법을 사용하여 $f$가 적분가능함을 보이자. $L(f,P) \leq R(f,P) \leq U(f,P)$을 활용한다.

(a): $f$ 가 연속인 경우 $R(f,P) = U(f,P)$를 만족하는 태그 $\{c_k\}^n_{k=1}$ 가 존재함을 보여라.

각 부분구간 $I_k=[x_{k-1},x_k]$ 는 닫히고 유계인 구간이다. $f$가 연속이므로 최대·최소 정리에 의해 $f$는 $I_k$ 에서 최댓값과 최솟값을 갖는다.  
따라서 어떤 $c_k^+,c_k^-\in I_k$가 존재하여 $f(c_k^+)=M_k,\ f(c_k^-)=m_k$ 이다.

태그를 $c_k=c_k^+$로 선택하면

$$R(f,P) =\sum_{k=1}^n f(c_k^+)\Delta x_k =\sum_{k=1}^n M_k\Delta x_k
=U(f,P)$$

따라서 $R(f,P)=U(f,P)$ 가 되게 하는 태그가 존재한다.

마찬가지로 $c_k=c_k^-$ 를 선택하면 $R(f,P)=L(f,P)$ 가 된다.

(b):  $f$가 연속이 아닌 경우, $R(f,P)=L(f,P)$를 만족하는 태그가 없을수도 있다. 하지만, $U(f,P)-R(f,P) < \epsilon$을 만족하는 $P$에 대한 태그가 존재함을 증명하라

연속이 아닐 때에는 상한 $M_k$ 가 실제 함수값으로 달성되지 않을 수 있다. 그러나 상한의 정의에 따라 임의의 $\eta>0$에 대하여 어떤 $c_k\in I_k$가 존재하여 $M_k-\eta<f(c_k)\le M_k$ 이다.

$\epsilon>0$ 이 주어졌다고 하고 $\eta=\frac{\epsilon}{b-a}$ 로 둔다. 각 부분구간에서 위 조건을 만족하도록 태그 $c_k$를 선택하면 $0\le M_k-f(c_k)<\eta.$ 따라서

$$
0 \le U(f,P)-R(f,P) =\sum_{k=1}^n\bigl(M_k-f(c_k)\bigr)\Delta x_k\\
<\eta\sum_{k=1}^n\Delta x_k =\eta(b-a)=\epsilon
$$

그러므로 $U(f,P)-R(f,P)<\epsilon$ 가 되도록 태그를 선택할 수 있다.

하한에 대해서도 하한의 정의에 따라 태그 $d_k\in I_k$를 $m_k\le f(d_k)<m_k+\eta$ 가 되게 선택할 수 있다. 그러면 $R(f,P)-L(f,P)<\epsilon$ 이다.

---

문제 5: 정리 전체의 증명

$(\Rightarrow)$ 리만 적분 가능하면 리만 합이 적분값으로 수렴한다

$$I:=\int_a^b f=A$$

라고 하자. 임의의 $\epsilon>0$을 택한다. 다르부 적분가능성 판정법에 따라 다음을 만족하는 분할 $P_\epsilon$이 존재한다.

$$U(f,P_\epsilon)-L(f,P_\epsilon)<\frac{\epsilon}{3}.$$

$P_\epsilon$의 부분구간 개수를 $n$이라 하자. 또한 $f$가 유계이므로 어떤 $M>0$이 존재하여

$$|f(x)|\le M \quad (x \in[a,b])$$

다음과 같이 둔다. $\delta=\frac{\epsilon}{9nM}.$ 이제 $(P,\{c_k\})$를 임의의 $\delta$-세분 태그된 분할이라 하고

$$P'=P\cup P_\epsilon$$

으로 둔다.

문제 1(b)에 의해 $U(f,P')-L(f,P')<\frac{\epsilon}{3}.$  
문제 2와 문제 3에 의해 $L(f,P')-\frac{\epsilon}{3} <L(f,P)$ 이고 $U(f,P)<U(f,P')+\frac{\epsilon}{3}$ 이다.  
문제 1(a)에 의해 $L(f,P)\le R(f,P)\le U(f,P)$ 이며 $L(f,P')\le I\le U(f,P')$ 이다.

따라서 $R(f,P)$와 $I$는 모두 다음 열린 구간 안에 들어간다.

$$\left( L(f,P')-\frac{\epsilon}{3}, U(f,P')+\frac{\epsilon}{3} \right).$$

이 구간의 길이는

$$\begin{aligned}
\left(U(f,P')+\frac{\epsilon}{3}\right)
-\left(L(f,P')-\frac{\epsilon}{3}\right)
&=U(f,P')-L(f,P')+\frac{2\epsilon}{3}\\
&<\frac{\epsilon}{3}+\frac{2\epsilon}{3} =\epsilon.
\end{aligned}$$

따라서 두 점 $R(f,P)$와 $I$ 사이의 거리는 $\epsilon$보다 작다. 즉,

$$|R(f,P)-I|<\epsilon.$$

$I=A$이므로

$$\boxed{|R(f,P)-A|<\epsilon}$$

$(\Leftarrow)$ 모든 충분히 세분된 리만 합이 $A$에 가까우면 적분 가능하다

다음 조건을 가정한다.

$$\forall\epsilon>0;\exists\delta>0:
\quad
P\text{가 }\delta\text{-세분이면 }
|R(f,P)-A|<\epsilon$$

가 모든 태그 선택에 대해 성립한다.

$f$가 리만 적분 가능함을 보이기 위해 임의의 $\epsilon>0$을 택한다. 가정에서 오차를 $\epsilon/4$로 적용하면 어떤 $\delta>0$이 존재하여 모든 $\delta$-세분 태그된 분할에 대해

$$|R(f,P)-A|<\frac{\epsilon}{4}$$

망의 크기가 $\delta$보다 작은 분할 $P$를 하나 선택한다. 예를 들어 충분히 큰 자연수 $N$에 대하여

$$\frac{b-a}{N}<\delta$$

가 되게 하고, $[a,b]$을 $N$등분하면 된다.

문제 4(b)에 의해 다음을 만족하는 태그를 선택할 수 있다.

$$U(f,P)-R^+(f,P)<\frac{\epsilon}{4}, \\R^-(f,P)-L(f,P)<\frac{\epsilon}{4}.$$

여기서 $R^+$는 상합에 가까운 태그를 이용한 리만 합이고, $R^-$는 하합에 가까운 태그를 이용한 리만 합이다.

두 리만 합은 같은 $\delta$-세분 분할에서 만들어졌으므로 가정에 의해

$$|R^+(f,P)-A|<\frac{\epsilon}{4},\quad |R^-(f,P)-A|<\frac{\epsilon}{4}$$

이다. 따라서

$$U(f,P)-L(f,P)\\=[U(f,P)-R^+(f,P)] +[R^+(f,P)-A]+[A-R^-(f,P)] +[R^-(f,P)-L(f,P)] $$

중간의 두 항은 음수일 수도 있으므로 절댓값으로 위에서 추정하면

$$U(f,P)-L(f,P) \\
\le [U(f,P)-R^+(f,P)] +|R^+(f,P)-A| +|A-R^-(f,P)| +[R^-(f,P)-L(f,P)] \\
<\frac{\epsilon}{4} +\frac{\epsilon}{4} +\frac{\epsilon}{4} +\frac{\epsilon}{4} =\epsilon.
$$

따라서 임의의 $\epsilon>0$에 대하여 $U(f,P)-L(f,P)<\epsilon$ 인 분할 $P$가 존재한다. 다르부 적분가능성 판정법에 의해 $f$는 리만 적분 가능하다.

이제 그 적분값을 $I=\int_a^b f$ 라고 하자. $I=A$임을 보여야 한다.

이미 증명한 $(\Rightarrow)$에 의해 임의의 $\eta>0$에 대하여 충분히 세분된 모든 태그된 분할은

$$|R(f,P)-I|<\frac{\eta}{2}$$

를 만족한다. 한편 처음 가정에 의해서도 충분히 세분된 모든 태그된 분할은

$$|R(f,P)-A|<\frac{\eta}{2}$$

를 만족한다.

두 조건을 동시에 만족할 만큼 세분된 태그된 분할 $P$를 택하면

$$|I-A| \le |I-R(f,P)|+|R(f,P)-A|\\
<\frac{\eta}{2}+\frac{\eta}{2}=\eta $$

$\eta>0$이 임의이므로 $I=A$ 따라서

$$\boxed{\int_a^b f=A}$$

이다. 이로써 필요충분조건의 양방향 증명이 완성된다. $\square$


## 게이지와 $\delta(x)$-세분
일반화된 리만 적분의 핵심은 '리만적분에 대한 극한 판정법'의 $\delta$를 $x$에 대한 함수로 보는 것이다.

### 정의. 게이지 (gauge)
모든 $x \in [a,b]$에 대해 $\delta(x) >0$이면 함수 $\delta: [a,b] \to \mathbb R$를 $[a,b]$에서의 게이지(gauge)라 한다.

### 정의. $\delta(x)$-세분
주어진 게이지 $\delta(x)$와 모든 부분구간 $[x_{k-1}, x_k]$에 대해 $x_k - x_{k-1} < \delta(c_k)$ 이면 태그된 분할 $(P, \{c_k\}^n_{k=1})$을 $\delta(x)$-세분 ($\delta(x)$-fine) 이라고 한다. 즉, 각 부분구간 $[x_{k-1}, x_k]$의 너비는 $\delta(c_k)$보다 작다.

- 분할의 정밀성 (fineness) 를 측정할 수 있게 해줌

### 정리. $\delta(x)$-세분 존재성
구간 $[a,b]$에서 게이지 $\delta(x)$가 주어질 때, $\delta(x)$-세분인 태그된 분할 $(P, \{c_k\}^n_{k=1})$이 존재한다.

**증명**

이를 이분법을 이용하여 증명한다.

1. 좋은 구간과 나쁜 구간

부분구간 $I=[u,v]\subseteq[a,b]$에 $\delta(x)$-세분 태그된 분할이 존재하면 $I$를 **좋은 구간**이라고 하자.  
반대로 그러한 분할이 존재하지 않으면 $I$를 **나쁜 구간**이라고 하자.  
증명하려는 것은 $[a,b]$가 좋은 구간이라는 사실이다.

2. 귀류법의 가정

반대로 $[a,b]$가 나쁜 구간이라고 가정하자. 이를 $I_0=[a,b]$ 라고 쓰겠다.

$I_0$를 중점에서 이등분하면 두 개의 닫힌구간이 생긴다.

만약 두 구간이 모두 좋은 구간이라면, 각 구간에서 얻은 $\delta(x)$-세분 태그된 분할을 합쳐서 $I_0$ 전체의 $\delta(x)$-세분 태그된 분할을 만들 수 있다.

그러면 $I_0$가 좋은 구간이 되어 가정과 모순이다. 따라서 두 구간 중 적어도 하나는 나쁜 구간이다. 그 나쁜 구간을 $I_1$이라고 하자.

같은 과정을 반복한다. $I_1$을 이등분하면 그중 적어도 하나는 나쁜 구간이다. 이를 $I_2$라고 한다.

이렇게 계속하면 다음과 같은 닫힌구간의 열을 얻는다. $I_0\supseteq I_1\supseteq I_2\supseteq\cdots$

각 $I_n$은 나쁜 구간이고, 길이는 $|I_n|=\frac{b-a}{2^n}$이다.

3. 구간들이 한 점으로 모인다

구간들이 서로 포함되는 닫힌구간이고 그 길이가 $|I_n|=\frac{b-a}{2^n}\longrightarrow0$ 이므로 축소구간 정리에 따라 모든 $I_n$에 공통으로 들어 있는 유일한 점 $c\in[a,b]$가 존재한다.

즉, $\bigcap_{n=0}^{\infty}I_n=\{c\}.$

특히 모든 $n$에 대하여 $c\in I_n$ 이다.

4. 게이지의 양수 조건을 사용한다

$\delta$는 게이지이므로 $\delta(c)>0$ 이다.

한편 $|I_n|=\frac{b-a}{2^n}\longrightarrow0$ 이므로 충분히 큰 $N$을 선택하면 $|I_N|<\delta(c)$ 가 된다.

$I_N=[u_N,v_N]$이라고 하자. 이 구간 전체를 하나의 부분구간으로 보고 태그를 $c$로 선택한다.

$$P_N=\{u_N,v_N\},\qquad c_1=c.$$

앞에서 $c\in I_N$임을 알고 있으므로 $c$는 올바른 태그이다. 또한

$$v_N-u_N=|I_N|<\delta(c)=\delta(c_1).$$

따라서 $(P_N,\{c\})$는 $I_N$의 $\delta(x)$-세분 태그된 분할이다.

그러므로 $I_N$은 좋은 구간이다.

그러나 $I_N$은 구성상 나쁜 구간이었다. 이는 모순이다.

따라서 최초의 가정, 즉 $[a,b]$가 나쁜 구간이라는 가정이 틀렸다. 그러므로 $[a,b]$에는 반드시 $\delta(x)$-세분인 태그된 분할이 존재한다. $\square$

## 일반화된 리만 적분가능성
'리만적분에 대한 극한 판정법'은 기존 리만 적분가능성 정의와 동치였다. 이젠 새로운 적분 값을 정의하는 방법을 소개한다

### 정의. 일반화된 리만 적분값 $A$ (generalized Riemann integral $A$)
다음이 성립할 때 함수 $f$가 구간 $[a,b]$에서 일반화된 리만 적분값 $A$ (generalized Riemann integral $A$) 를 가진다고 한다:

임의의 $\epsilon >0$에 대하여 어떤 게이지 $\delta(x)$가 $[a,b]$에서 존재하여, $\delta(x)$-세분인 모든 태그된 분할 $(P, \{c_k\}^n_{k=1})$에 대하여 다음이 성립한다:

$$|R(f,P)-A| < \epsilon$$

### 정리. 일반화된 리만 적분값의 유일성
함수가 일반화된 리만 적분값을 가질 때, 그 적분값은 유일하다.

>**증명**
>
>함수 $f$가 구간 $[a,b]$에서 두 개의 일반화된 리만 적분값 $A$와 $B$ ($A \neq B$)를 가진다고 가정하자.
>
>$\epsilon = \frac{|A-B|}{3} > 0$이라고 놓자.
>
>정의에 의해, 이 $\epsilon$에 대하여 어떤 게이지 $\delta_1(x)$, $\delta_2(x)$가 존재하여, $\delta_1(x)$-세분, $\delta_2(x)$-세분인 모든 태그된 분할 $(P, \{c_k\})$에 대하여
>$$|R(f,P)-A| < \epsilon, \quad |R(f,P)-B| < \epsilon$$
>가 성립한다.
>
>이제 $\delta(x) = \min\{\delta_1(x), \delta_2(x)\}$로 정의하면, $\delta(x)$도 게이지이다. 보조정리(세분된 분할의 존재)에 의해 $\delta(x)$-세분인 태그된 분할 $(P, \{c_k\})$이 존재한다.
>
>이 분할에 대하여:
>$$|A - B| = |A - R(f,P) + R(f,P) - B| \leq |A - R(f,P)| + |R(f,P) - B| < \epsilon + \epsilon = \frac{2|A-B|}{3}$$
>
>따라서
>$$|A - B| < \frac{2|A-B|}{3}$$
>
>이는 모순이다. 그러므로 $A = B$이어야 한다. $\square$

### 정리. 리만적분과 일반화된 리만적분의 관계
리만적분 가능하여 $\int_a^b f = A$인 함수는 항상 일반화된 리만적분값 $A$를 가진다. 역은 성립하지 않는다.

>**증명**
>
>$f$가 리만적분 가능하고 $\int_a^b f = A$라고 하자.
>
>임의의 $\epsilon > 0$에 대하여 리만 적분에 대한 극한 판정법에 의해, 어떤 $\delta_0 > 0$이 존재하여 모든 $\delta_0$-세분인 태그된 분할 $(P, \{c_k\})$에 대하여 $|R(f,P) - A| < \epsilon$ 이 성립한다.
>
>이제 게이지 $\delta(x)$를 모든 $x \in [a,b]$에 대하여 $\delta(x) = \delta_0$로 정의하면 (상수함수), 이는 $[a,b]$에서의 게이지이다.
>
>$\delta(x)$-세분인 태그된 분할 $(P, \{c_k\})$은 정의에 의해 $x_k - x_{k-1} < \delta(c_k) = \delta_0$을 모든 부분구간에 대해 만족하므로, $\delta_0$-세분이다.
>
>따라서 리만 적분에 대한 극한 판정법에 의해
>$$|R(f,P) - A| < \epsilon$$
>
>이는 일반화된 리만 적분값의 정의를 만족하므로, $f$는 구간 $[a,b]$에서 일반화된 리만 적분값 $A$를 가진다. $\square$
>
>역이 성립하지 않는 이유:
>일반화된 리만 적분은 각 점 $x$에 따라 서로 다른 게이지 $\delta(x)$를 사용할 수 있기 때문에 리만 적분보다 더 큰 함수 집합에 적용할 수 있다. 예를 들어 디리클레 함수는 리만 적분 불가능하지만, 특별히 구성된 게이지를 사용하면 일반화된 리만 적분값을 가질 수 있다.
>
>---
>**디리클레 함수의 일반화된 리만 적분가능성**
>
>구간 $[0,1]$의 유리수들을 $\mathbb Q\cap[0,1]=\{r_1,r_2,r_3,\ldots\}$ 처럼 나열하자. 그리고 함수 $g:[0,1]\to\mathbb R$를
>
>$$
> g(x)=
>\begin{cases}
>1, & x\in \mathbb Q\cap[0,1],\\
>0, & x\notin \mathbb Q
>\end{cases}
>$$
>
>로 정의한다. 이 함수는 디리클레 함수이며, 리만 적분은 불가능하다. 그러나 일반화된 리만 적분에서는 적분값 0을 가진다.
>
>임의의 $\epsilon>0$을 택하고, 게이지를 아래와 같이 정의하자:
>
>$$
>\delta(x)=
>\begin{cases}
>\dfrac{\epsilon}{2^{j+1}}, & x=r_j,\\[2mm]
>1, & x\notin\mathbb Q
>\end{cases}
>$$
>
>이제 $(P,\{c_k\}_{k=1}^n)$를 임의의 $\delta(x)$-세분 태그된 분할이라고 하자. 리만 합은
>
>$$
>R(g,P)=\sum_{k=1}^n g(c_k)(x_k-x_{k-1})
>$$
>
>태그 $c_k$가 무리수이면 $g(c_k)=0$이므로, 그 부분구간은 리만 합에 아무런 기여를 하지 않는다. 따라서 유리수 태그를 가진 부분구간만 생각하면 된다.
>
>만약 $c_k=r_j$라면, $\delta(x)$-세분 조건에 의해
>
>$$
> x_k-x_{k-1}<\delta(r_j)=\frac{\epsilon}{2^{j+1}}
>$$
>
>이 성립한다. 한 점 $r_j$는 태그된 분할에서 많아야 두 부분구간의 태그로 사용될 수 있다. 실제로 어떤 부분구간의 내부에 속하거나, 서로 인접한 두 부분구간의 공통 끝점이 될 수 있기 때문이다.
>
>따라서 $r_j$가 태그인 부분구간들의 전체 길이는
>
>$$
>2\delta(r_j)=\frac{\epsilon}{2^j}
>$$
>
>보다 작다. 그러므로
>
>$$ 0 \le R(g,P) <\sum_{j=1}^{\infty} 2\delta(r_j) =\sum_{j=1}^{\infty}\frac{\epsilon}{2^j} =\epsilon $$
>
>즉, $|R(g,P)-0|<\epsilon$ 이다.  
>이는 모든 $\delta(x)$-세분 태그된 분할에 대해 성립하므로, $g$는 일반화된 리만 적분 가능하고 그 적분값은
>
>$$
>\int_0^1 g = 0
>$$
>
>이다.
>
>이 예는 일반화된 리만 적분이 리만 적분보다 더 넓은 함수군에 적용된다는 점을 잘 보여준다. 리만 적분은 모든 점에서 균일한 오차를 요구하지만, 일반화된 리만 적분은 각 점마다 다른 게이지를 허용하므로, 무리수 점에서는 0으로, 유리수 점에서는 아주 작은 구간만 허용하는 방식으로 적분을 정의할 수 있다.

## 일반화된 리만 적분의 미적분학 기본정리 재구성
일반화된 리만 적분을 이용하면 도함수의 적분가능성 가정이 필요없어진다.
또, 증명과정에서 평균값 정리가 필요없어진다.

### 정리. 미적분학의 기본정리
$F:[a,b] \to \mathbb R$가 $[a,b]$의 모든 점에서 미분가능하다 가정하고 $f(x) = F'(x)$로 두자. 이때 $f$는 다음과 같은 일반화된 리만 적분값을 가진다:

$$\int_a^b f = F(b)- F(a)$$

**증명**

먼저 증명에서 주의할 점이 하나 있다. 미분가능성에서 얻는 국소 오차를 그대로 $\epsilon$으로 놓으면 마지막 합의 오차는 $\epsilon(b-a)$가 된다. 최종 오차를 $\epsilon$보다 작게 만들려면 국소 오차를 $\eta=\frac{\epsilon}{b-a}$ 로 놓아야 한다.

문제 11 

분할 $P={x_0,x_1,\ldots,x_n}, \quad a=x_0<x_1<\cdots<x_n=b$ 에 대하여 $F(b)-F(a) = \sum_{k=1}^n\bigl[F(x_k)-F(x_{k-1})\bigr]$ 임을 보여야 한다.

우변을 전개하면

$$
\sum_{k=1}^n\bigl[F(x_k)-F(x_{k-1})\bigr] =[F(x_1)-F(x_0)]\ +[F(x_2)-F(x_1)]\ +\cdots\ +[F(x_n)-F(x_{n-1})] \\
=F(x_n)-F(x_0).
$$

$x_0=a$, $x_n=b$ 이므로 증명 완료.

문제 12

각 $c\in[a,b]$에 대하여 모든 $0<|x-c|<\delta(c)$ 인 $x\in[a,b]$가

$$
\left|
\frac{F(x)-F(c)}{x-c}-f(c)
\right|<\eta
$$

를 만족하도록 하는 $\delta(c)>0$가 존재함을 보여야 한다.

여기서 $\eta>0$은 임의의 양수이다. 정리의 최종 증명에서는 $\eta=\frac{\epsilon}{b-a}$ 로 선택한다.

$F$가 $c$에서 미분 가능하고 $F'(c)=f(c)$ 이므로 미분계수의 정의에 따라

$$
\lim_{x\to c} \frac{F(x)-F(c)}{x-c} =f(c)
$$

극한의 $\epsilon$-$\delta$ 정의를 적용하면, 주어진 $\eta>0$에 대하여 어떤 $\delta(c)>0$가 존재하여 $0<|x-c|<\delta(c)$ 이면

$$
\left|
\frac{F(x)-F(c)}{x-c}-f(c)
\right|<\eta
$$

각 점 $c$에 대하여 이렇게 얻은 양수 $\delta(c)$를 대응시키면 $\delta:[a,b]\to(0,\infty)$ 가 된다. 따라서 $\delta$는 $[a,b]$ 에서의 게이지이다. 즉,

$$
0<|x-c|<\delta(c) \Longrightarrow \left|\frac{F(x)-F(c)}{x-c}-f(c)\right|<\eta
$$

가 성립하는 게이지를 구성할 수 있다.  
$c=a$ 또는 $c=b$인 경우에는 $[a,b]$ 안에서 접근하는 한쪽 극한을 사용한다.

문제 13

문제 12에서 구성한 게이지에 대하여 $(P,{c_k}_{k=1}^n)$ 를 임의의 $\delta$-세분 태그된 분할이라고 하자. 따라서 $c_k\in[x_{k-1},x_k]$ 이고 $x_k-x_{k-1}<\delta(c_k)$ 이다.

(a) 태그에서 양쪽 끝점까지의 오차

먼저 $x_k\ne c_k$라고 하자. 태그가 부분구간 안에 있으므로

$$0<x_k-c_k \le x_k-x_{k-1} <\delta(c_k)$$

문제 12의 부등식에서 $c=c_k$, $x=x_k$로 놓으면

$$
\left|\frac{F(x_k)-F(c_k)}{x_k-c_k}  -f(c_k) \right|<\eta.
$$

양변에 $x_k-c_k>0$을 곱하면

$$|F(x_k)-F(c_k)-f(c_k)(x_k-c_k)| <\eta(x_k-c_k)$$

를 얻는다.

마찬가지로 $x_{k-1}\ne c_k$이면

$$
0<c_k-x_{k-1} \le x_k-x_{k-1} <\delta(c_k).
$$

문제 12의 부등식에서 $x=x_{k-1}$로 놓으면

$$
\left| \frac{F(x_{k-1})-F(c_k)} {x_{k-1}-c_k} -f(c_k) \right|<\eta.
$$

양변에 $c_k-x_{k-1}=|x_{k-1}-c_k|$를 곱하면

$$
\boxed{
|F(c_k)-F(x_{k-1})
-f(c_k)(c_k-x_{k-1})|
<\eta(c_k-x_{k-1})
}
$$


태그가 끝점인 경우: 만약 $c_k=x_k$이면 첫 번째 부등식의 양변이 모두 0이다. 이때는 엄밀하게 $0\le0$ 이므로 등호를 포함한 부등식을 사용해야 한다.

마찬가지로 $c_k=x_{k-1}$이면 두 번째 식의 양변이 모두 0이다. 따라서 모든 경우를 한꺼번에 쓰면

$$
|F(x_k)-F(c_k)-f(c_k)(x_k-c_k)|
\le\eta(x_k-c_k)
$$

및

$$
|F(c_k)-F(x_{k-1})
-f(c_k)(c_k-x_{k-1})|
\le\eta(c_k-x_{k-1})
$$

이다. 태그가 해당 끝점과 다를 때에는 부등호가 엄격하다.

(b) 한 부분구간 전체의 오차

다음 차를 생각한다.

$$
F(x_k)-F(x_{k-1}) -f(c_k)(x_k-x_{k-1})
$$

태그 $c_k$를 중간에 삽입하면 $F(x_k)-F(x_{k-1}) = [F(x_k)-F(c_k)] +[F(c_k)-F(x_{k-1})]$ 이고 $x_k-x_{k-1} =(x_k-c_k)+(c_k-x_{k-1})$ 이다.

따라서

$$
F(x_k)-F(x_{k-1})-f(c_k)(x_k-x_{k-1}) \\
= \bigl[F(x_k)-F(c_k)-f(c_k)(x_k-c_k)\bigr] + \bigl[F(c_k)-F(x_{k-1})
-f(c_k)(c_k-x_{k-1})\bigr]$$

삼각부등식을 적용하면

$$
|F(x_k)-F(x_{k-1}) -f(c_k)(x_k-x_{k-1})| \\
\le  |F(x_k)-F(c_k)-f(c_k)(x_k-c_k)| + |F(c_k)-F(x_{k-1}) -f(c_k)(c_k-x_{k-1})| \\
\le \eta(x_k-c_k)+\eta(c_k-x_{k-1}) = \eta(x_k-x_{k-1})
$$

부분구간의 길이가 양수이므로 두 구간 $[x_{k-1},c_k], [c_k,x_k]$ 중 적어도 하나는 길이가 양수이다. 그쪽에서는 문제 12의 엄격한 부등식이 성립하므로 최종적으로 $|F(x_k)-F(x_{k-1}) -f(c_k)(x_k-x_{k-1})| <\eta(x_k-x_{k-1})$  를 얻는다.

---

전체 증명

$(F:[a,b]\to\mathbb R)$가 모든 점에서 미분 가능하고 $f(x)=F'(x)$

라고 하자. $a < b$라고 가정한다. $a = b$인 경우에는 양변이 모두 $0$이므로 자명하다.

일반화된 리만 적분의 정의에 따라 다음을 보여야 한다: $\forall\epsilon>0;\exists\text{ 게이지 }\delta$ 가 존재하여, 모든 $\delta$-세분 태그된 분할 $(P,\{c_k\})$에 대해

$$
|R(f,P)-[F(b)-F(a)]|<\epsilon
$$

이 성립해야 한다.

임의의 $\epsilon>0$을 택하고 $\eta=\frac{\epsilon}{b-a}$ 로 둔다.

$F$가 각 $c\in[a,b]$에서 미분 가능하므로 문제 12에 의해 각 $c$마다 어떤 $\delta(c)>0$가 존재하여

$$
0<|x-c|<\delta(c) \Rightarrow \left| \frac{F(x)-F(c)}{x-c}-f(c)\right|<\eta
$$

이다. 이와 같이 구성한 $\delta$는 게이지이다.

이제 $(P,\{c_k\}_{k=1}^n)$를 임의의 $\delta$-세분 태그된 분할이라고 하자. 정리 8.1.5에 의해 이러한 태그된 분할은 실제로 존재한다.

문제 11에 의해

$$
F(b)-F(a) = \sum_{k=1}^n[F(x_k)-F(x_{k-1})]
$$

이다. 한편

$$
R(f,P)  = \sum_{k=1}^n f(c_k)(x_k-x_{k-1})
$$

이다. 따라서

$$
|F(b)-F(a)-R(f,P)|\\
= \left| \sum_{k=1}^n \left[ F(x_k)-F(x_{k-1}) -f(c_k)(x_k-x_{k-1}) \right] \right|\\
\le \sum_{k=1}^n \left| F(x_k)-F(x_{k-1}) -f(c_k)(x_k-x_{k-1}) \right|.
$$

문제 13(b)를 적용하면

$$
|F(b)-F(a)-R(f,P)| < \sum_{k=1}^n\eta(x_k-x_{k-1})\\
=\eta\sum_{k=1}^n(x_k-x_{k-1}) =\eta(b-a)
$$

$\eta=\epsilon/(b-a)$이므로 $|F(b)-F(a)-R(f,P)| < \frac{\epsilon}{b-a}(b-a) =\epsilon$

즉, 임의의 $\delta$-세분 태그된 분할에 대하여

$$
\boxed{
|R(f,P)-[F(b)-F(a)]|<\epsilon
}
$$

이다.

따라서 일반화된 리만 적분의 정의에 의해 $f=F'$는 일반화된 리만 적분 가능하고

$$
\boxed{
\int_a^b f=F(b)-F(a)
}
$$

이다. $\square$


### 정리. 일반화된 리만적분 기반 치환적분법(change-of-variable)
함수 $g:[a,b] \to \mathbb R$가 $[a,b]$의 각 점에서 미분가능하고 $F$가 집합 $g([a,b])$에서 미분가능하다고 가정하자. 모든 $x \in g([a,b])$에 대해 $f(x)=F'(x)$이면 다음이 성립한다:

$$\int_a^b (f \circ g)\cdot g' = \int_{g(a)}^{g(b)}f$$

**증명**
> $g$는 미분 가능하므로 연속이다. 따라서 중간값정리에 의해 $g([a,b])$는 구간이다. 특히 $g(a)$와 $g(b)$ 사이의 모든 점은 $g([a,b])$에 포함된다.
>
> 이제 합성함수 $H=F\circ g$ 를 생각하자. $g$는 $[a,b]$에서 미분 가능하고 $F$는 $g([a,b])$에서 미분 가능하므로, 연쇄법칙에 의해 $H$는 $[a,b]$에서 미분 가능하며
>
> $$
> H'(x) =(F\circ g)'(x) =F'(g(x))g'(x) =f(g(x))g'(x) =(f\circ g)(x)g'(x)
> $$
>
> 정리 8.1.9에 따르면 미분 가능한 함수의 도함수는 일반화된 리만 적분 가능하고, 그 적분값은 원시함수의 양 끝점 함수값의 차이다. 이를 $H=F\circ g$에 적용하면
>
> $$
> \int_a^b(f\circ g)(x)g'(x) dx =\int_a^bH'(x) dx =H(b)-H(a) =F(g(b))-F(g(a)) \tag{1}
> $$
>
> 한편 $f=F'$ 이므로 정리 8.1.9를 (F)에 적용하면
>
> $$
> \int_{g(a)}^{g(b)}f(u),du
> =F(g(b))-F(g(a)).
> \tag{2}
> $$
>
> 식 (1)과 식 (2)의 우변이 같으므로
>
> $$
> \boxed{
> \int_a^b(f\circ g)(x)g'(x) dx
> = \int_{g(a)}^{g(b)}f(u),du
> }
> $$
>
> 이다. $\square$
>
>$g(a)>g(b)$인 경우에는 
>
>$$\int_{g(a)}^{g(b)}f =-\int_{g(b)}^{g(a)}f$$
>
>라는 유향적분의 약속을 사용한다. 이때도
>
>$$
>\int_{g(a)}^{g(b)}f =F(g(b))-F(g(a))
>$$
>
>이므로 같은 증명이 성립한다.
>
>또한 $g(a)=g(b)$이면
>
>$$
>\int_{g(a)}^{g(b)}f=0
>$$
>
>이고
>
>$$
>\int_a^b(f\circ g)g'=F(g(b))-F(g(a))=0
>$$
>
>이므로 역시 성립한다.
>
>이 증명의 핵심은 다음 두 식이 같은 값으로 계산된다는 점이다.
>
>$$
>\int_a^b(F\circ g)'  =F(g(b))-F(g(a)) =\int_{g(a)}^{g(b)}F'
>$$
>
>따라서 $g$가 단조이거나 일대일일 필요가 없다. $g$가 중간에 여러 번 증가하고 감소하더라도 연쇄법칙과 정리 8.1.9에 의해 결과가 성립한다.
>